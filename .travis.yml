language: php

php:
  - 7.2
  - 7.3
  - 7.4
  - hhvm

before_script:
  - composer self-update
  - composer install --prefer-source --no-interaction

script: ./vendor/bin/phpunit

matrix:
  allow_failures:
    - php: hhvm
  fast_finish: true
