language: php

php:
  - 7.0
  - 7.1
  - 7.2
  - 7.3
  - 7.4
  - hhvm

before_script:
  - composer self-update
  - composer install --prefer-source --no-interaction --dev

script: ./vendor/bin/phpunit

matrix:
  allow_failures:
    - php: hhvm
  fast_finish: true
